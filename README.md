# Learning-Typescript

initial setup
npm init -->generate package.json file and add script commands in package.json file
compile
watch
start


npm install lite-server to install the lite server
tsc -init


to run as web application, 
use
npm run compile
npm run start

to run ts file as standalone file

npx ts-node <filename.ts>
